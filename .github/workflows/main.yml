name: go_test
on: push 
jobs:
  go_build: 
    -uses : actions/checjout@v3
    -name: setup go
    uses: actions/seup-go@v3
      with:
      go-version: '^1.13.1' # The Go version to download (if necessary) and use.
  - run: go run helloworld.go
